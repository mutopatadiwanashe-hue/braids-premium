const revealObserver=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>revealObserver.observe(el));
const cards=document.querySelectorAll('.parallax-wrap');function parallax(){const vh=innerHeight;cards.forEach(card=>{const img=card.querySelector('.parallax-img');const r=card.getBoundingClientRect();const progress=(vh/2-(r.top+r.height/2))/(vh/2);const y=Math.max(-28,Math.min(28,progress*28));img.style.transform=`translateY(${y}px) scale(1.08)`})}addEventListener('scroll',parallax,{passive:true});addEventListener('resize',parallax);parallax();
const glow=document.querySelector('.cursor-glow');addEventListener('pointermove',e=>{glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px'});
const input=document.getElementById('photoInput'),preview=document.getElementById('preview');input.addEventListener('change',()=>{const file=input.files[0];if(!file)return;if(!file.type.startsWith('image/'))return;const url=URL.createObjectURL(file);preview.innerHTML=`<img src="${url}" alt="Uploaded hairstyle preview">`;});
const menu=document.querySelector('.menu');menu.addEventListener('click',()=>{const nav=document.querySelector('.site-header nav');nav.style.display=nav.style.display==='flex'?'none':'flex';if(innerWidth<=800){nav.style.position='absolute';nav.style.top='66px';nav.style.left='0';nav.style.right='0';nav.style.padding='20px 7vw';nav.style.background='rgba(247,245,241,.97)';nav.style.flexDirection='column';nav.style.alignItems='flex-start'}});
